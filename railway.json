{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && PORT=5000 BASE_PATH=/ pnpm --filter @workspace/movie-marketplace run build && BASE_PATH=/admin/ pnpm --filter @workspace/admin-dashboard run build && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "node artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
